// export const filterPokemon = ({ pokemon }) => {
//   const filteredPokemon = pokemon.filter((pokemon) => {
//     const pokemonType =
//       types.length === 0 || types.every((type) => pokemon.type.includes(type));
//     const pokemonWeakness =
//       weaknesses.length === 0 ||
//       weaknesses.every((weakness) => pokemon.weaknesses.includes(weakness));

//     return pokemonType && pokemonWeakness;
//   });

//   return filteredPokemon;
// };
